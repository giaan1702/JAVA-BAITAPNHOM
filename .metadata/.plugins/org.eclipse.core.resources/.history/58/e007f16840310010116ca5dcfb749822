package UI;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.*;
import javax.swing.*;

public class AdminDashboard extends JFrame implements ActionListener {
	private int AdminID;
    private JPanel navPanel;
    private JButton bookRoomButton;
    private JButton viewCustomerButton;
    private JButton paymentButton;
    private JButton adduserButton;
    private JButton addphongButton;
    private JButton addsophong;
    private JButton logoutButton;
    private JPanel footerPanel;
    private JPanel mainPanel;
    private CardLayout cardLayout;

    // Panels
    private JPanel bookRoomPanel;
    private JPanel viewCustomerJPanel;
    private JPanel paymentPanel;
    private JPanel adduserPanel;
    private JPanel addphongPanel;
    private JPanel addsophongPanel;
    
    public AdminDashboard() {
    	setTitle("Hệ thống Quản lý Khách sạn - Trang Admin");
        setSize(600, 400);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLocationRelativeTo(null);
        setLayout(new BorderLayout());

        cardLayout = new CardLayout();
        mainPanel = new JPanel(cardLayout);

        // Khởi tạo các panel (có thể thay bằng panel thực)
        bookRoomPanel = new JPanel();
        viewCustomerJPanel = new JPanel();
        paymentPanel = new JPanel();
        adduserPanel = new JPanel();
        addphongPanel = new JPanel();
        addsophongPanel = new JPanel();

        // Đăng ký các panel vào mainPanel
        mainPanel.add(bookRoomPanel, "bookRoom");
        mainPanel.add(viewCustomerJPanel, "bookingHistory");
        mainPanel.add(paymentPanel, "payment");
        mainPanel.add(adduserPanel, "adduser");
        mainPanel.add(addphongPanel, "addphong");
        mainPanel.add(addsophongPanel, "addsophong");

        // Tạo các nút
        bookRoomButton = new JButton("🛏 Đặt phòng");
        viewCustomerButton = new JButton("📅 Danh sách khách hàng");
        paymentButton = new JButton("💳 Thanh toán");
        adduserButton = new JButton("📅 Thêm User");
        addphongButton = new JButton("📅 Thêm Phòng");
        addsophong = new JButton("📅 Thêm Số Phòng");
        logoutButton = new JButton("Đăng xuất");

        // Căn chỉnh nút
        Dimension buttonSize = new Dimension(200, 40);
        JButton[] buttons = {bookRoomButton, viewCustomerButton, paymentButton,adduserButton,addphongButton,addsophong};
        for (JButton btn : buttons) {
            btn.setMaximumSize(buttonSize);
            btn.setAlignmentX(Component.CENTER_ALIGNMENT);
        }

     // Panel menu bên trái
        navPanel = new JPanel();
        navPanel.setLayout(new BoxLayout(navPanel, BoxLayout.Y_AXIS));
        navPanel.setBorder(BorderFactory.createEmptyBorder(10, 10, 10, 10)); // Thêm padding

        // Phân nhóm chức năng
        JLabel group1Label = new JLabel("Quản lý Đặt phòng");
        group1Label.setFont(new Font("Arial", Font.BOLD, 12));
        group1Label.setBorder(BorderFactory.createEmptyBorder(5, 0, 5, 0));
        navPanel.add(group1Label);

        // Các nút chức năng chính
        bookRoomButton = new JButton("🛏 Đặt phòng");
        viewCustomerButton = new JButton("👥 Danh sách KH");
        paymentButton = new JButton("💳 Thanh toán");

        // Phân nhóm hệ thống
        navPanel.add(Box.createVerticalStrut(15));
        JLabel group2Label = new JLabel("Quản lý Hệ thống");
        group2Label.setFont(new Font("Arial", Font.BOLD, 12));
        group2Label.setBorder(BorderFactory.createEmptyBorder(5, 0, 5, 0));
        navPanel.add(group2Label);

        // Các nút quản lý hệ thống
        adduserButton = new JButton("👤 Thêm User");
        addphongButton = new JButton("🏨 Thêm Loại Phòng");
        addsophong = new JButton("🔢 Thêm Số Phòng");

        // Thiết lập style chung cho nút
        Dimension buttonSize = new Dimension(200, 40);
        Color btnColor = new Color(240, 240, 240);
        JButton[] buttons = {bookRoomButton, viewCustomerButton, paymentButton, 
                            adduserButton, addphongButton, addsophong};

        for (JButton btn : buttons) {
            btn.setMaximumSize(buttonSize);
            btn.setAlignmentX(Component.CENTER_ALIGNMENT);
            btn.setBackground(btnColor);
            btn.setFocusPainted(false);
            btn.setBorder(BorderFactory.createCompoundBorder(
                BorderFactory.createLineBorder(Color.LIGHT_GRAY),
                BorderFactory.createEmptyBorder(5, 15, 5, 15)
            ));
        }

        // Thêm các thành phần vào panel
        navPanel.add(bookRoomButton);
        navPanel.add(Box.createVerticalStrut(8));
        navPanel.add(viewCustomerButton);
        navPanel.add(Box.createVerticalStrut(8));
        navPanel.add(paymentButton);

        navPanel.add(Box.createVerticalStrut(15));
        navPanel.add(group2Label);
        navPanel.add(Box.createVerticalStrut(8));
        navPanel.add(adduserButton);
        navPanel.add(Box.createVerticalStrut(8));
        navPanel.add(addphongButton);
        navPanel.add(Box.createVerticalStrut(8));
        navPanel.add(addsophong);
        navPanel.add(Box.createVerticalGlue());

        // Nút đăng xuất ở dưới cùng
        JPanel logoutPanel = new JPanel();
        logoutPanel.setLayout(new BoxLayout(logoutPanel, BoxLayout.X_AXIS));
        logoutPanel.add(Box.createHorizontalGlue());
        logoutPanel.add(logoutButton);
        logoutPanel.setBorder(BorderFactory.createEmptyBorder(15, 0, 0, 0));

        navPanel.add(logoutPanel);

        // Gắn ActionListener
        bookRoomButton.addActionListener(this);
        viewCustomerButton.addActionListener(this);
        paymentButton.addActionListener(this);
        adduserButton.addActionListener(this);
        addphongButton.addActionListener(this);
        addsophong.addActionListener(this);
        logoutButton.addActionListener(this);

        add(mainPanel, BorderLayout.CENTER);

        JLabel welcomeLabel = new JLabel("Chào mừng, Admin!", SwingConstants.CENTER);
        mainPanel.add(welcomeLabel, "home");
        cardLayout.show(mainPanel, "home");

        setVisible(true);
    }
	@Override
	public void actionPerformed(ActionEvent e) {
		if (e.getSource() == bookRoomButton) {
	           new Booking1().setVisible(true);
	        } else if (e.getSource() == viewCustomerButton) {
	            new ViewCustomer().setVisible(true);
	        } else if (e.getSource() == paymentButton) {
	            new Payment().setVisible(true);
	        } else if (e.getSource() == adduserButton) {
	            new Payment().setVisible(true);
	        } else if (e.getSource() == addphongButton) {
	            new Payment().setVisible(true);
	        } else if (e.getSource() == addsophong) {
	            new Payment().setVisible(true);
	        } else if (e.getSource() == logoutButton) {
	            dispose();
	            new Login().setVisible(true);
	        }
		
	}
	public static void main(String[] args) {
    	new AdminDashboard();
          
    }

}
